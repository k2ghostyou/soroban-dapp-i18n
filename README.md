# soroban-dapp-i18n
an internationalization toolkit for Stellar/Soroban dApps (translated UI strings, locale-aware amount/date formatting, and RTL support). Build it incrementally in git, committing after each logical unit of work is complete and verified — not as one giant dump.
